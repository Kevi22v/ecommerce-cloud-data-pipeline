# ==========================================
# 1. THE CART GENERATOR (The Spammer)
# ==========================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-generator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-generator
  template:
    metadata:
      labels:
        app: cart-generator
      annotations:                     
        prometheus.io/scrape: "true"   
        prometheus.io/port: "5000"   
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: generator
        image: vivek22v/ecommerce-generator:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        command: ["python", "cart_gen.py"]
        envFrom:
        - configMapRef:
            name: ecommerce-config
        # Note: SLEEP_MIN/MAX are now handled by the TURBO mode logic inside the Python script
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:            
            cpu: "250m"      
            memory: "250Mi"  

---
# ==========================================
# 2. THE ORDER CONVERTER (The Math Engine)
# ==========================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-generator
spec:
  replicas: 1 
  selector:
    matchLabels:
      app: order-generator
  template:
    metadata:
      labels:
        app: order-generator
      annotations:                     
        prometheus.io/scrape: "true"   
        prometheus.io/port: "5000" 
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: generator
        image: vivek22v/ecommerce-generator:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        command: ["python", "order_gen.py"]
        envFrom:
        - configMapRef:
            name: ecommerce-config
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:            
            cpu: "250m"      
            memory: "250Mi"  

---
# ==========================================
# 3. THE DELIVERY FULFILLER (Logistics)
# ==========================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery-generator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: delivery-generator
  template:
    metadata:
      labels:
        app: delivery-generator
      annotations:                     
        prometheus.io/scrape: "true"   
        prometheus.io/port: "5000"     
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: generator
        image: vivek22v/ecommerce-generator:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        command: ["python", "delivery_gen.py"]
        envFrom:
        - configMapRef:
            name: ecommerce-config
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "250Mi"

---
# ==========================================
# 4. THE DATA PROCESSOR (PySpark)
# ==========================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-processor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: processor
  template:
    metadata:
      labels:
        app: processor
      annotations:                     
        prometheus.io/scrape: "true"   
        prometheus.io/port: "4040"     
        prometheus.io/path: "/metrics/prometheus"
    spec:
      containers:
      - name: processor
        image: vivek22v/ecommerce-processor:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 4040 
        envFrom:
        - configMapRef:
            name: ecommerce-config
        env:
          - name: DB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: ecommerce-secrets
                key: DB_PASSWORD
          - name: AWS_ACCESS_KEY_ID
            valueFrom:
              secretKeyRef:
                name: ecommerce-secrets
                key: AWS_ACCESS_KEY
          - name: AWS_SECRET_ACCESS_KEY
            valueFrom:
              secretKeyRef:
                name: ecommerce-secrets
                key: AWS_SECRET_KEY
        resources:
          requests:
            cpu: "150m"
            memory: "256Mi"
          limits:
            cpu: "400m"      
            memory: "400Mi"